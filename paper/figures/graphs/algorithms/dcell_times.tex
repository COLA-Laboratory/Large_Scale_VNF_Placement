\documentclass[a4paper]{standalone}
\usepackage{xcolor}
\usepackage{pgfplots}
\usepackage{tikz}

\usetikzlibrary{patterns}
\usepgfplotslibrary{groupplots}

\begin{document}
\begin{tikzpicture}

    \begin{axis}[
            major x tick style = {opacity=0},
            xtick=data,
            xtick pos=left,
            ylabel style={
                yshift=-2ex,
            },
            symbolic x coords={500,1000,2000,4000,8000,16000},
            xminorgrids,
            minor x tick num = 1,
            xlabel = {Number of servers},
            ymode = log,
            ylabel = Wall clock time (s),
            width=\textwidth,
            height=0.4\textwidth,
            line width = 1pt,
        ]

        % PPLS-Simple
        \addplot [blue, mark=x]
        table[x index = 0, y index = 2, col sep = comma, y error minus index = 1, y error plus index = 3]
            {../../../../processed/algorithm_comparison/FatTree/PPLS_Simple/time.csv};
        % PPLS/D
        \addplot [red, mark=+]
        table[x index = 0, y index = 2, col sep = comma, y error minus index = 1, y error plus index = 3]
            {../../../../processed/algorithm_comparison/FatTree/PPLSD/time.csv};
        % MOEA/D
        \addplot [orange, mark=square]
        table[x index = 0, y index = 2, col sep = comma, y error minus index = 1, y error plus index = 3]
            {../../../../processed/algorithm_comparison/FatTree/MOEAD/time.csv};
        % IBEA
        \addplot [purple, mark=|]
        table[x index = 0, y index = 2, col sep = comma, y error minus index = 1, y error plus index = 3]
            {../../../../processed/algorithm_comparison/FatTree/IBEA/time.csv};
        % NSGA-II
        \addplot [brown, mark=o]
        table[x index = 0, y index = 2, col sep = comma, y error minus index = 1, y error plus index = 3]
            {../../../../processed/algorithm_comparison/FatTree/NSGA-II/time.csv};

    \end{axis}
\end{tikzpicture}
\end{document}